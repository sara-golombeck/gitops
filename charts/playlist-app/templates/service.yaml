apiVersion: v1
kind: Service
metadata:
  name: {{ include "zelda-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zelda-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: {{ .Values.service.name | quote }}
  selector:
    {{- include "zelda-app.selectorLabels" . | nindent 4 }}