# infra-apps/values/elasticsearch-values.yaml
global:
  kibanaEnabled: true
master:
  masterOnly: false
  replicaCount: 1
  heapSize: "1g"
  resources:
    requests:
      cpu: "0.5"
      memory: "2Gi"
    limits:
      cpu: "1"
      memory: "3Gi"
  readinessProbe:
    enabled: false
  livenessProbe:
    enabled: false
data:
  replicaCount: 0
ingest:
  enabled: false
coordinating:
  replicaCount: 0
volumeClaimTemplate:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 30Gi
  storageClassName: "gp3"
persistence:
  labels:
    enabled: true
security:
  enabled: true
  elasticPassword: "9sRLSqZOhtUpSpyn"  
  tls:
    restEncryption: true
    autoGenerated: true
kibana:
  ingress:
    enabled: false
  elasticsearch:
    security:
      tls:
        enabled: true
        verificationMode: "none"
        usePemCerts: false
      auth:
        enabled: true
        kibanaPassword: "9sRLSqZOhtUpSpyn"  
        createSystemUser: true
        elasticsearchPasswordSecret: "elasticsearch"
  livenessProbe:
    enabled: false
  readinessProbe:
    enabled: false